name: ci

on:
  pull_request:
    branches:
      - "**"

jobs:
  lint_test:
    uses: babylonlabs-io/.github/.github/workflows/reusable_node_lint_test.yml@push
    with:
      run-build: true
      run-unit-tests: true
